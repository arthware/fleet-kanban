{
  "shortcuts": [
    {
      "label": "Run",
      "command": "npm run dogfood",
      "icon": "play"
    },
    {
      "label": "Dev (full stack)",
      "command": "node scripts/dev-full.mjs",
      "icon": "play"
    }
  ],
  "worktree": {
    "postCreateCommand": "(npm ci --no-audit --no-fund || npm install --no-audit --no-fund) && (npm --prefix web-ui ci --no-audit --no-fund || npm --prefix web-ui install --no-audit --no-fund)"
  }
}
