{
  "shortcuts": [
    {
      "label": "Run",
      "command": "npm run dogfood",
      "icon": "play"
    },
    {
      "label": "Dev (full stack)",
      "command": "node scripts/dev-full.mjs",
      "icon": "play"
    }
  ],
  "worktree": {
    "postCreateCommand": "npm --prefix web-ui ci --no-audit --no-fund || npm --prefix web-ui install --no-audit --no-fund",
    "unsharedPaths": [
      "dist",
      "build",
      "out",
      ".next",
      ".turbo",
      ".cache",
      ".vite",
      ".parcel-cache",
      ".svelte-kit",
      ".nuxt",
      ".output",
      "*.tsbuildinfo"
    ]
  }
}
